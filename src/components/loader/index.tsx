import React from 'react';
import { LoaderDiv, LoaderSubDiv } from '../styledComponents';

const Loader = () => {
  return (
    <LoaderDiv>
        <LoaderSubDiv></LoaderSubDiv>
    </LoaderDiv>
  )
}

export default Loader